import React from "react";

const ExamForm = () => {
  return (
    <div
      class="exam_form"
      style={{ paddingBlock: "10px", borderRadius: "10px" }}
    >
      <p style={{ borderRadius: "3px", fontSize: "12px", fontWeight: "bold" }}>
        Reappear Exam Form Portal is closed
      </p>
    </div>
  );
};

export default ExamForm;
